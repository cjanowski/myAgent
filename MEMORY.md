# MEMORY

Standing notes for the agent across sessions. Preferences and decisions worth re-applying — not a log of everything that happened.

Prune this every so often. Stale entries here are worse than no entry: the agent will apply outdated context confidently.

## Conventions
<!-- e.g. "API errors always return {error: {code, message}}" -->

## Standing decisions
<!-- e.g. "We use Zod for runtime validation, not manual checks" -->

## Known gotchas in this codebase
<!-- e.g. "The `users` table has a legacy `email` column that's unused — use `primary_email`" -->
